FROM alpine:3.20

RUN apk add --no-cache logrotate tzdata \
    && mkdir -p /var/lib/logrotate

ENV TZ=Asia/Shanghai

CMD ["sh", "-c", "while true; do logrotate -s /var/lib/logrotate/logrotate.status /etc/logrotate.conf; sleep 3600; done"]
