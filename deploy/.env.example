# Compose project settings (nginx publish port).
# Backend runtime: copy .env.backend.example to .env.backend
# Frontend: no runtime env file; NEXT_PUBLIC_API_BASE_URL is a build arg (default empty).
HTTP_PORT=80
