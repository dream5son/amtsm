# Compose-only settings (nginx publish port).
# Application env files are NOT duplicated here:
#   backend  → copy backend/.env.example  to backend/.env
#   frontend → copy frontend/.env.example to frontend/.env
HTTP_PORT=80
